# Set up local configuration.

Need to run `../installer.sh`.

All configuratins should be in `~/.config/home-manager` to manage in git.

## Nix

Edit `~/.config/home-manager`. Nothing special.

## vim

Edit `~/.config/home-manager/vim/local.vim`.
