# vim: set filetype=zsh :

DOTFILES_PATH="$HOME/dotfiles"

if [[ -f "$DOTFILES_PATH/zshenv" ]]; then
 source "$DOTFILES_PATH/zshenv"
fi

if [[ -f "$HOME/.zshenv.custom" ]]; then
 source "$HOME/.zshenv.custom"
fi
