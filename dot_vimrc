" .vimrc

scriptencoding utf-8

let s:vim_files=[
   \'~/dotfiles/vim/vim_settings.vim',
   \'~/dotfiles/vim/vim_plugins.vim',
   \'~/.vim/local_settings.vim'
   \]

for s:file in s:vim_files
 let s:path = expand(s:file)
 if filereadable(s:path)
  execute 'source '.s:path
 endif
endfor

" vim: set ft=vim :
